import React, { useEffect, useState } from "react";
import { StyleSheet, Text, View } from "react-native";

const App = () => {
    const [BookResponses, setBookResponses] = useState([]);
    
	return (
		<View>
			<Text>Textello World</Text>
		</View>
	);
};

export default App;

const styles = StyleSheet.create({});
